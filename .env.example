# Copy this file to .env and fill in real values. NEVER commit .env.
# Get a Gemini key: https://aistudio.google.com/apikey  (should start with "AIza…")
GEMINI_API_KEY='your-gemini-api-key'

# Fallback / failover provider (optional but recommended). https://console.groq.com/keys  (starts with "gsk_")
GROQ_API_KEY='your-groq-api-key'

# Optional: only needed if you enable hard-case escalation to Claude/GPT. https://openrouter.ai/keys
# OPENROUTER_API_KEY='your-openrouter-api-key'
